openpyxl==3.1.5
Django==5.2.7
reportlab==4.0.4
# ReportLab necesita PIL para incrustar PNG (el logo institucional del
# encabezado). Sin Pillow, ImageReader._read_image() llama Image.open() sobre
# None y TODA generacion de PDF falla. Estaba en el venv local pero no aqui,
# asi que los PDF venian rotos en Docker.
Pillow==12.2.0
# HTML→PDF de la planilla de asignación. Python puro; reutiliza ReportLab.
xhtml2pdf==0.2.17
segno==1.6.6
python-dotenv
mysqlclient
gunicorn==23.0.0
whitenoise==6.9.0
dj-database-url==2.3.0
pytest>=8.0
pytest-django>=4.8
